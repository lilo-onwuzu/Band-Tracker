#if ($bands.size() != 0)
  #foreach ($band in $bands)
    <a href="/bands/$band.getId()">$band.getName()</a><br>
  #end
#else
  <p><em>No bands have been added to the list!</em><p>
#end

<a href="/bands/new">Add New Band</a>
<a href="/venues">All Venues</a>
